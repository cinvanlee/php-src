// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("cinvan", "for cinvan support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("cinvan", "enable cinvan support", "no");

if (PHP_CINVAN != "no") {
	EXTENSION("cinvan", "cinvan.c");
}

